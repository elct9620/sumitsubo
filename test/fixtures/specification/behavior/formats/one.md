# One

A feature in the format this build really carries.

## Includes

- `test/init_test.rb`

## `F-001` A feature read by the format this build carries

| Step | Statement |
| --- | --- |
| Given | a directory holding more than one format |
| When | the directory is loaded |
| Then | the feature written in each of them is read |
