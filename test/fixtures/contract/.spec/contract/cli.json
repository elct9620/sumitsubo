{
  "name": "CLI",
  "description": "The commands the executable answers.",
  "marker": "@command",
  "include": ["src/*.rb"],
  "contracts": [
    { "name": "init", "description": "Lay down an empty specification." },
    { "name": "verify", "description": "Check the source against the specification." }
  ]
}
