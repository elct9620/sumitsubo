{
  "name": "One",
  "description": "A feature in the format this build really carries.",
  "include": ["test/init_test.rb"],
  "scenarios": [
    {
      "id": "F-001",
      "title": "A feature read by the format this build carries",
      "given": ["a directory holding more than one format"],
      "when": "the directory is loaded",
      "then": "the feature written in each of them is read"
    }
  ]
}
