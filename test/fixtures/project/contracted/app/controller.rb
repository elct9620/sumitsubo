# @route GET /users/:id
def show
end

# The run reads this file because the routes definition includes it. The CLI
# definition does not, so this claim names a contract it cannot implement.
# @command verify
def verify_here
end
