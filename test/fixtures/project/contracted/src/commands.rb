# One contract, two ways in — the second is an entrance the specification
# does not describe.
# @command init
def init_here
end

# @command init
def init_there
end

# Nothing registers this one, so there is nothing to compare it against.
# @command render
def render_here
end
