{
  "language": "ruby", "name": "Routes",
  "description": "A file that meant to register routes and lost its marker.",
  "include": ["app/*.rb"],
  "contracts": [
    { "name": "GET /users/:id", "description": "One user." }
  ]
}
