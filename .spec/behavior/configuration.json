{
  "name": "Configuration",
  "description": "Where a run is configured from, and what the configuration decides.",
  "notes": [
    {
      "type": "paragraph",
      "text": [
        "Walking up for the nearest configuration is what tsc and RuboCop both",
        "do: each searches the ancestor directories until one answers."
      ]
    },
    {
      "type": "paragraph",
      "text": [
        "The base a configuration is read against and the one findings answer",
        "from are deliberately different. The first is what lets a run started",
        "anywhere under it reach the same files; the second is what lets a",
        "reader go straight to a finding."
      ]
    },
    {
      "type": "paragraph",
      "text": [
        "`verify: false` keeps a specification the project means to hold without",
        "a run being checked against it yet."
      ]
    },
    {
      "type": "paragraph",
      "text": [
        "`.spec` is the default because `spec/` is already RSpec's, and because",
        "a directory that does not start with a dot is one a build is liable to",
        "sweep in as source."
      ]
    }
  ],
  "include": [
    "test/config_test.rb"
  ],
  "scenarios": [
    {
      "id": "C-001",
      "title": "The nearest .sumi.json decides the base",
      "given": [
        "a .sumi.json at the top of a project",
        "a run starting several directories below it"
      ],
      "when": "the configuration is loaded",
      "then": "the base is the directory holding that .sumi.json, however deep the run started"
    },
    {
      "id": "C-002",
      "title": "With no .sumi.json, the repository it sits in",
      "given": [
        "a project with a .git but no .sumi.json"
      ],
      "when": "the configuration is loaded",
      "then": "the base is the repository root, whether .git is a directory or a gitfile"
    },
    {
      "id": "C-003",
      "title": "With neither, where the run started",
      "given": [
        "a directory with no .sumi.json and no .git above it"
      ],
      "when": "the configuration is loaded",
      "then": "the base is where the run started"
    },
    {
      "id": "C-004",
      "title": "A .sumi.json that will not parse",
      "given": [
        "a .sumi.json that is not readable JSON"
      ],
      "when": "the configuration is loaded",
      "then": "the file is named as unreadable"
    },
    {
      "id": "C-005",
      "title": "Only the exceptions are listed",
      "given": [
        "a .sumi.json naming one specification as not to be verified"
      ],
      "when": "the configuration is asked about each specification",
      "then": "the specification it named is not verified"
    },
    {
      "id": "C-006",
      "title": "A specification nobody mentioned is verified",
      "given": [
        "a .sumi.json naming one specification as not to be verified"
      ],
      "when": "the configuration is asked about each specification",
      "then": "a specification the file does not mention is verified"
    },
    {
      "id": "C-011",
      "title": "What the project leaves alone",
      "given": [
        "a configuration naming what to exclude"
      ],
      "when": "the configuration is loaded",
      "then": "it answers the rules once for the project, and a configuration naming none answers none"
    },
    {
      "id": "C-012",
      "title": "What the .gitignore beside it already said",
      "given": [
        "a project keeping a .gitignore"
      ],
      "when": "the configuration is loaded",
      "then": "what git leaves out is excluded too"
    },
    {
      "id": "C-013",
      "title": "A path put back, the configuration being read after the .gitignore",
      "given": [
        "a project keeping a .gitignore that leaves a path out",
        "a .sumi.json whose exclude names that path with a `!`"
      ],
      "when": "the configuration is loaded",
      "then": "the path is left in"
    },
    {
      "id": "C-014",
      "title": "The .gitignore switched off",
      "given": [
        "a project keeping a .gitignore",
        "a .sumi.json switching the .gitignore off"
      ],
      "when": "the configuration is loaded",
      "then": "what git leaves out is not excluded"
    }
  ]
}
