{
  "glossary": [
    {
      "include": ["CLAUDE.md", "README.md", ".spec/contract/*.json", ".spec/behavior/*.json", "sumitsubo/**/*.rb", "docs/glossary.md"],
      "terms": [
        {
          "term": "Specification",
          "definition": "What a project declares its source should stay aligned with. It is the structured files; what Render writes from them is a Document.",
          "not": []
        },
        {
          "term": "Structured Specification",
          "definition": "The machine-readable file a mechanism reads: .spec/glossary.json, one file per kind of contract under .spec/contract/, and one file per feature under .spec/behavior/.",
          "not": []
        },
        {
          "term": "Document",
          "definition": "What Render writes for a person to read: the half of the specification that says what it means, without what the tool needs in order to find things.",
          "not": []
        },
        {
          "term": "Verifiable Specification",
          "definition": "The part of the structured specification a mechanism can check against source code.",
          "not": []
        },
        {
          "term": "Source Code",
          "definition": "The code verified against the specification. Glossary and Contract verify the implementation, Behavior the tests.",
          "not": []
        },
        {
          "term": "Syntax Tree",
          "definition": "What tree-sitter answers for a source file: every token kept, comments included.",
          "not": [
            {
              "term": "AST",
              "reason": "An abstract tree drops the comments, which are the only thing Glossary reads."
            }
          ]
        },
        {
          "term": "Contract",
          "definition": "An interface a project registers as one it means to keep, claimed by the source that implements it. The marker it is claimed with is its namespace.",
          "not": []
        },
        {
          "term": "Behavior",
          "definition": "A scenario the specification declares in a BDD style, claimed by the test that implements it.",
          "not": [
            { "term": "behaviour", "reason": "Behavior is the spelling every identifier here uses." },
            { "term": "Behaviour", "reason": "Behavior is the spelling every identifier here uses." },
            { "term": "behaviours", "reason": "Behaviors is the spelling every identifier here uses." }
          ]
        }
      ]
    }
  ]
}
