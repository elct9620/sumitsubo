/build/
/vendor/
/.packages/*/build/
# The revision a build answers for, written by scripts/vendor.sh.
/build_rev.rb
# ruby-lsp writes an index wherever it is run, fixtures included. It excludes
# itself with a generated .gitignore; saying so here does not depend on that.
.ruby-lsp/
# Notes taken while working, kept out of the history on purpose.
/tmp/
